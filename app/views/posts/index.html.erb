<%= link_to "Create new post", new_post_path %>
<h1>Posts#index</h1>
<% @posts.each do |post| %>
  <% if post.user == current_user || current_user.friends.include?(post.user) %>
    <%= link_to post.title, post %>
  <% else %>
    <% next %>
  <% end %>
<% end %>
